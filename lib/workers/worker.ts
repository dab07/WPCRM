// Worker module removed — Redis/BullMQ dependency has been eliminated.
// Job processing (lifecycle-scan, weather-scan) is now triggered directly
// from API routes without a queue intermediary.
export {};
